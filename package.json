{
  "name": "api_experiencias",
  "version": "1.0.0",
  "description": "Hoja de vida profesional con panel administrativo React",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "npm run install:client && npm run build:client",
    "build:client": "cd client && npm run build",
    "install:client": "cd client && npm install",
    "dev:all": "concurrently \"npm run dev\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "crud",
    "nodejs",
    "express",
    "postgresql",
    "experiencias-profesionales",
    "react",
    "portfolio"
  ],
  "author": "Luis Felipe",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "path": "^0.12.7",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.10"
  }
}
